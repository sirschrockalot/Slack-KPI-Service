apiVersion: v1
kind: ConfigMap
metadata:
  name: aircall-slack-config
  namespace: aircall-slack
data:
  PORT: "3000"
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  # Add other non-sensitive configuration here