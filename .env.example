# Aircall API Configuration
AIRCALL_API_ID=your_aircall_api_id_here
AIRCALL_API_TOKEN=your_aircall_api_token_here

# Slack Configuration
SLACK_API_TOKEN=xoxb-your-slack-bot-token-here
SLACK_CHANNEL_ID=C1234567890

# Application Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration (comma-separated list of allowed origins)
# Leave empty to allow localhost only (default: http://localhost)
# Production example: ALLOWED_ORIGINS=https://yourdomain.com,https://api.yourdomain.com
ALLOWED_ORIGINS=http://localhost,http://localhost:3000,http://localhost:6000

# Users to exclude from reports (comma-separated)
EXCLUDED_USERS=Joel Schrock,Test User

# Agent categorization (comma-separated lists)
# Dispo agents: KPI = 60+ dials/day AND 60+ minutes talk time/day
DISPO_AGENTS=Derek,Lyka
# Acquisition agents: KPI = 50+ dials/day AND 3.5+ hours talk time/day
ACQUISITION_AGENTS=Alex

# Timezone (adjust for your location)
TZ=America/Chicago

# JWT Authentication Configuration
# IMPORTANT: Must be at least 32 characters for security
# Generate with: openssl rand -base64 32
JWT_SECRET=your_generated_secret_here

# JWT Token Expiration (optional, in seconds or string format like '1h', '7d', '30d')
# If not set, tokens never expire (not recommended for production)
# Recommended: 1h for access tokens, implement refresh token mechanism
# JWT_EXPIRATION=1h

# JWT Versioning (for zero-downtime secret rotation)
# Uncomment to enable multi-version JWT support
# Step 1: Add new secret as JWT_SECRET_V2
# Step 2: Set JWT_ACTIVE_VERSION=v2 (new tokens use v2)
# Step 3: Set deprecation date for v1
# Step 4: After deprecation date, remove JWT_SECRET_V1
# JWT_SECRET_V1=your_old_secret_here_min_32_chars
# JWT_SECRET_V2=your_new_secret_here_min_32_chars
# JWT_ACTIVE_VERSION=v2
# JWT_DEPRECATED_DATE_V1=2026-02-28