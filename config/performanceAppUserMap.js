/**
 * Aircall user -> presidential-performance-hub identifiers.
 *
 * Required fields per mapping:
 * - aircallUserName
 * - userId (DailyKpiEntry.userId)
 * - teamId (DailyKpiEntry.teamId)
 *
 * Optional:
 * - repName (for logging only)
 * - teamName (for logging only)
 */
module.exports = [
  // Example:
  // {
  //   aircallUserName: 'Derek',
  //   userId: 'app-user-uuid-or-id',
  //   teamId: 'app-team-uuid-or-id',
  //   repName: 'Derek',
  //   teamName: 'Dispo'
  // }
];

